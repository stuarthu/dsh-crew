# QA 测试计划：T-06（`<job-slug>` 的形状）

写这份计划的时间点：**读任何代码之前**。只读了 `~/.dsh/crew/jobs/<job-slug>/dod.md`（版本 12）和三份 CRD。

覆盖的验收检查：44、45、46。

## 用例

| # | 检查 | 我做什么 | 必须发生什么 | 文件 |
| --- | --- | --- | --- | --- |
| 1 | 44 | 读 `roles/pm.md` 第 6 步那一段，逐条找：只允许小写字母、数字和 `-`；不许以 `-` 开头或结尾；不许 `..`；一个具体的长度上限数字；以及「为什么」（值会被拼进文件路径和 shell 命令，PM 自己的会话是被 guard 信任的） | 五点全在，长度上限是一个真的数字 | `T-06/case-01-slug-shape-rule.mjs` |
| 2 | 45 | 在同一段里找「用户给的名字不合形状时怎么办」 | 说明 PM 自己转成合规 slug，并把转换结果告诉用户；不照原样用 | `T-06/case-02-slug-nonconforming-handling.mjs` |
| 3 | 46 | 把仓库需要的部分复制到临时目录，从副本的 `roles/pm.md` 里删掉被钉住的那条 slug 规则字符串，跑 `node tools/verify-mount.mjs` | 非 0 退出，输出里点名 `roles/pm.md`；不改时退出 0 | `T-06/case-03-slug-assertion-pinned.mjs` |

## 不能在这里跑 / 只能部分验证的

- 检查 44 的「写清为什么」是散文，用例只能钉住关键词（文件路径、shell 命令、信任）。
  措辞质量由 doc-reviewer 判。
