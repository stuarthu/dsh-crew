# 可测性缺口清单（常备）

这个文件说：**这个产品里有哪些东西，跑不出一个可执行用例来判。**

它是常备的，不是某一次作业的记录。所以按「是什么东西」分组，不按任务号分组——一年后
读到这里的人，看到 `T-05` 三个字什么也想不起来。

它从哪来：QA 的测试计划是一次性的，用例一写出来计划就没用了，作业结束就丢
（`docs/decisions/crd/0006-split-by-lifetime.md`）。但计划里有一段是持久的：**「测不到
什么、为什么」**。那一段搬到这里。所以规矩是——**丢掉一份计划之前，先把它那一段并进这个
文件**；如果这里已经有同一个缺口了，就不用再加一条，只在需要时把话补准。

怎么读每一条：**缺口**（测不到什么）、**为什么**、**该怎么办**、**状态**。

---

## 1. 「文档写得对不对」这类验收检查，没有任何用例能判

**缺口**：验收检查 8、9、10、21、47、53 全是关于文档措辞的——`README.md` 和
`README-zh.md` 说的是不是同一件事、一段话读起来清不清楚、一句解释是不是真的解释了。
QA 没有为它们写用例。

**为什么**：用例能断言「这个字符串在」，不能断言「这句话是对的、清楚的、够用的」。
把一个措辞检查写成用例，只会得到一个钉住某几个词的假绿灯。

**该怎么办**：这类检查要一个读者，不是一个用例——`crew-doc-reviewer`，或者用户自己读。
本次作业（`pm-merge-step`）里这六条**是 PM 一个人核的**，因为用户跳过了文档评审。
也就是说：这六条只有一个人看过，而这个人正是写它们的人。以后再碰到同一类检查，
默认要一个独立的读者。

**状态**：未关闭，而且不会被用例关闭。这是分工问题，不是技术问题。

## 2. `roles/*.md` 里的散文规则：用例只能钉字符串，判不了句子

**缺口**：角色提示词是散文。用例能证明 `git merge --no-ff`、`--ff-only`、
`git branch -d crew/`、`publishCheck` 这些关键串还在（`docs/qa/T-01/`、
`docs/qa/T-06/`），也能证明 `roles/pm.md` 里不出现 `{{`；但「这六条设计决定一条不漏地
写清了」（检查 3）、「写清了为什么」（检查 44）这种话，用例判不了。

**为什么**：意思是人读出来的，字符串不是。

**该怎么办**：两件事一起做。改措辞时跑一遍 `bash docs/qa/run-all.sh`——被钉住的串没了
就会红；然后让一个人读那段话。**红了不等于用例错了**：先读失败信息看它点名哪个串，
只有当文档真的按规矩改了（CRD 或 ADR 在），才改用例。

**状态**：未关闭，按设计如此。钉字符串是故意选的下限：它挡住「悄悄删掉一条规则」，
不声称能判文笔。

## 3. 验收检查逐字点名一个字符串时，QA 和工程师的输入必然重合

**缺口**：规矩是 QA 不抄工程师的测试。但有些验收检查本身就逐字写出了要送的命令
（例如检查 32 点名了 `push-ok` 的六种写法），照检查写用例，输入就和工程师的一样。

**为什么**：检查把输入定死了。剩下能不一样的只有壳子和边界——QA 自己搭挂载壳子、
自己加改名、软链接、`bash -c`、全路径删除这些工程师没送的输入。

**该怎么办**：别为了「不一样」去换掉检查点名的输入。把重合写下来，另外加自己挑的边界。

**状态**：未关闭，按设计如此。

## 4. `tools/verify-mount.mjs` 有一半会跳过：机器上没有 dsh 自己的那份包

**缺口**：`@deepseek-ai/dsh-tool-subagent` 装不了（它的 peer `@deepseek-ai/dsh-tasks`
没发布到公共 npm），所以**角色工具那一半检查会跳过**，CI 上也跳过。跳过时它大声说出来：

```
SKIP  role-tool mount checks: dsh is not reachable from here (...)
```

**为什么**：那一半要拿真的 schema 去校验每个角色的 allow / deny 配置。没有那个包，
没东西可校验。

**该怎么办**：本地做一次软链接就能拿到全量检查（`CLAUDE.md` 里也写了）：

```sh
mkdir -p node_modules/@deepseek-ai
ln -s ~/.dsh/profiles/node_modules/@deepseek-ai/dsh-tool-subagent \
      node_modules/@deepseek-ai/dsh-tool-subagent
```

**看到 `SKIP` 就当那一半没跑过**。改了任何角色的 allow / deny 之后，绿灯不代表配置对。

**状态**：未关闭。只要那个 peer 包没发布，CI 上就一直如此。

## 5. dsh 本身不在这个仓库里跑，所以运行时行为只能在「挂载」这一层验证

**缺口**：用例用假的 ctx 挂载 `host/crew.js`、`host/git-guard.js`，然后断言拼给 PM 的
提示词、boot log、抛出的错。真的跑一个作业时会发生什么，这里看不到。具体地：
检查 50 要求「旧的 `agentsPerJob` 被静默忽略」，用例只能证明**挂载不抛错、提示词里没有
它**；「运行时真的不再有单作业上限」要 dsh 真的跑一个作业才看得到。

**为什么**：这个仓库是 dsh 的插件，自己不会运行（`CLAUDE.md` 第一段）。

**该怎么办**：把这类结论按它真实的强度说——「提示词里没有」不是「运行时没有」。
要更强的证据，只能在真的 dsh 会话里跑一次，手工看。

**状态**：未关闭，是这个仓库的性质决定的。

## 6. 有些检查只能从源码结构判断（静态检查）

**缺口**：两类话跑不出行为来：

- 「正则只在挂载时建一次，不在每条命令的路径上建」（检查 29）——只能读源码，看
  `new RegExp` 在 `apply()` 里、不在处理每条命令的函数里。
- 「`try` / `finally` 在」（检查 33 的一半）。

**为什么**：这是「代码长什么样」，不是「代码做了什么」。同样的行为可以用别的写法达到，
静态检查会误红；写法对了也不保证行为对。

**该怎么办**：静态检查要**说出来它是静态的**，别当成行为证据。能补行为的就补：
检查 33 的另一半已经补上了——`docs/qa/T-05/case-07-temp-dir-cleanup.mjs` 故意让
`tools/verify-guard.mjs` 中途抛异常，然后看它建的临时目录还在不在。

**状态**：检查 33 的清理行为**已关闭**（由 `case-07-temp-dir-cleanup.mjs` 关闭）。
检查 29 的「只建一次」**仍是静态的**。

## 7. `case-26` 依赖 git 提交历史：浅克隆里它会红，而且答不了检查 11

**缺口**：`docs/qa/T-01/case-26-repo-diff-scope.mjs` 按提交标题里的任务标记搜**整个**
历史，核对「每个任务的提交只动这个任务拥有的文件」（检查 11）。在
`git clone --depth 1` 出来的浅克隆里，那些提交根本没被 fetch 下来，用例**红**，并逐条
打印 `not run`。

**它为什么读提交历史、不读工作树**：工作树只说「现在有哪些改动」，说不出「哪一行属于
哪个任务」——尤其是几个会话同时在改同一批文件的时候。检查 11 的原话是「按任务核对」，
只有提交才带着任务标记。

**为什么**：这是故意选的。另一个做法是「历史不在就当通过」，那会在一个空集合上打绿灯——
一个什么都没检查的绿灯比红灯更坏。所以它把「这里答不了」和「证据真的没了」分开说，
答不了的时候一个 `ok` 都不打。

**该怎么办**：读失败信息，它会告诉你是哪一种。要在这里跑，就
`git fetch --unshallow`（或者别用 `--depth` 克隆）。CI 用 `fetch-depth: 0`，所以 CI 上
历史是全的。**浅克隆里检查 11 无法验证**，不要把那次红当成缺陷。

**状态**：未关闭，按设计如此（宁可红得清楚）。

## 8. 部分克隆（`--filter=blob:none`）不会被认成「历史不可用」

**缺口**：`case-26` 只认两种「答不了」：不是 git 工作树、以及
`git rev-parse --is-shallow-repository` 说 true。**部分克隆**（`git clone
--filter=blob:none`）两条都不满足——提交在，blob 按需现取——所以它会照常往下跑，
一旦取不到对象，失败信息会长得像别的毛病。

**为什么**：这是一个 QA 任务报出来的发现，当时判为 optional（非阻塞），没有做。
真要认，得去看 promisor remote 的配置，比现在这两行判断复杂得多。

**该怎么办**：如果 `case-26` 在一个部分克隆里失败，先想到这一条，别去改用例检查什么。
要修的话，修的是「答不了」的判断，不是被检查的行为。

**状态**：未关闭（optional）。

## 9. 「整套项目测试全绿」这件事，故意没有 QA 用例

**缺口**：检查 1（`npm test` 四项全绿——写下这条时 `scripts.test` 里是四条命令，今天是
**六**条）和检查 18（`node tools/verify-guard.mjs` 全绿）没有写成 QA 用例。

**为什么**：写了就是把工程师的测试抄一遍。

**该怎么办**：直接跑，把真实输出贴进报告：

```sh
npm test                      # scripts.test 里的六条命令，用 && 依次跑
bash docs/qa/run-all.sh       # 只跑 QA 的用例（它是那六条里的第五条）
```

因为 `run-all.sh` 在 `npm test` 里面，QA 的用例是项目测试的一部分——**旧任务的用例
今天红了就是回归**，不是「旧文件过期了」。

**状态**：未关闭，按设计如此。

## 10. 收尾那道门没有封闭的判定词表：`code: maybe` 照样绿

**缺口**：`tools/verify-tasks.mjs` 检查 Verdicts 行的**形状**——一节一行、四个值都在、
每个 `not run` / `skipped` 各带理由、`changes needed` 点名任务号。它**不检查那个值是不是
一个合法的判定词**。所以下面这些全都是绿的：

```
- **Verdicts**：code: maybe ｜ security: 待办 ｜ qa: 大概吧 ｜ doc: ¯\_(ツ)_/¯
```

`docs/qa/T-42/case-20-gate-all-four-values.mjs` 里那条「四个 `pass` 是绿的」正好也证明了
这一点的另一半：门只看到「有一个值」。

**为什么没有用例**：写一条「`code: maybe` 必须红」的用例，等于要求先有一张封闭词表，
而**没有任何 DoD 条目要求过它**。T-40 的工程师自己把这件事报了出来，并且没有替规则做
决定：门是按 `CRD 0011` 写的。

**要说准一点：词表本身并不缺。** `roles/pm.md` 第 11 步已经把允许的写法逐条列了出来——
`code: pass`（或 `code: pass (round 2)`）、`security: pass` 或 `security: skipped — <理由>`、
`qa: pass`、`doc: pass`，没跑的写 `not run — <理由>`，`changes needed` 要点名 `T-<编号>`。
`pass with notes` 不在这张表里。
**2026-08-22 更正**：上面这份抄件原本还写着 `doc: skipped — the user asked for it`，
而 **T-66 把那个值删掉了**——它建立在一个 `roles/pm.md` 从没给过的许可上（全份文件里没有
一句说用户可以关掉文档评审），所以现在 `doc` **没有 `skipped` 值**：没人读过的文档写
`doc: not run — <理由>`，是**评审缺了**，不是**被豁免了**。这一处更正由 T-66 的 engineer
报回，PM 写（按新的「哪类文档谁写」表，这个文件是 PM 的）。**它不改变本条的状态**——
这份抄件是不是最新，和「门读不读这张表」是两件事。
所以缺的**不是那张表，是「把那张表变成门的一条规则」这个决定**：那张表今天是写给 PM 的
规矩，门一个字都不读。
真要变成门规则，还得先说清「值」和「理由」各自的边界——理由是自由文本（本仓库的 Verdicts
行就写着 `not run — 任务还在跑`），值不是。凭空放宽或收紧都会误红，所以这里留一个洞，
不留一条替用户拍板的用例。

**该怎么办**：要关它，先要一个决定——哪些词算合法判定词，写进 `CRD` 或 `principles.md`；
然后加一条门规则，再加一条 QA 用例。**在那之前，这一行只保证「写下来了」，不保证「写对
了」。** 检查脚本自己每次都把这句话打出来：「Passing is not the same as clean … it cannot
prove a review happened — a `code: pass` typed by the PM passes it」。

**2026-08-22 补**（`crew-qa-4` 报的行，PM 写）：第 10 步里「Verdicts 行仍然四个值、
没跑的写 `not run` 带理由、永不写 `pass`」这半截，改到这一天为止**一处机器检查都没有**——
它只活在 `tools/verify-mount.mjs` 那条失败信息的散文里，而没有任何一条 `includes()` 读它。
也就是说从第 10 步删掉 `doc:`、或者删掉「永不写 `pass`」，**全项目一处都不会红**。
现在由 `docs/qa/T-56/case-08-existing-pins-intact.mjs` 直接钉住：四个值各一条断言，
掉哪一个说哪一个（实测只删 `doc:` 时只有那一条红）。
**这不关闭本条**：钉住的是「提示词里写着这句话」，门读的还是形状，`code: maybe` 照样绿。
**这个洞比少一条断言更贵，所以写下来**：`tools/verify-tasks.mjs` 那道门**照旧**要求
`docs/design/tasks.md` 里四个值全在，所以提示词丢了这半截之后，PM 会撞上一道红门、
而它的提示词里没有任何一句话教它怎么写——**门还在，说明书没了**。

**状态**：未关闭，等一个决定（不是等代码）。

## 11. 按全路径 grep 查不出裸文件名的过期指针

**谁报的**：`crew-qa-5`，2026-08-22，在 A7 改名之后（PM 写下这一条）。

**是什么**：A7 把 `docs/design/prd.md` 改成 `docs/design/prd-<日期>-<作业 slug>.md`，
`hld` 同形。改名的验法是 `grep -c 'docs/design/prd\.md\|docs/design/hld\.md'`，
**它只看带路径的写法**。而 `docs/qa/T-51/case-14-gaps-records-crew-qa-hole.mjs` 第 9 行写着

```
// prove", hld.md section 5): ...
```

——**裸文件名，没有 `docs/design/` 前缀**。它是一处真指针（在告诉读者去哪读那条要求），
指向一个今天已经不存在的名字，而那条验法命中不了它。

**为什么它值得单独一条**：这不是「漏了一处」，是**验法的形状漏了一整类**。
本作业的 T-70 和 T-77 各自撞过同一件事的另一半——它们的 DoD 只要求改带路径的那几处，
两个 engineer 都自己发现文件里还有裸的 `hld.md`、报上来问，PM 才把那一格的范围扩成
「带路径的和裸文件名的都算」。**那两次是 engineer 顶回来的，不是验法抓到的**；
这一次也是。

**该怎么办**：凡是改文件名的作业，除了按全路径 grep，**再按裸名 grep 一遍**
（`grep -rn 'prd\.md\|hld\.md'`），然后逐处读上下文判它是**指针**还是**提及**
（这条分辨本身写在 `docs/design/prd-2026-08-21-apply-req.md` 的 DoD 第 11 条 v6 里，
本作业用了四次）。

**状态**：未关闭。那一行今天还指向空处，留给最后一轮 QA 或下一件作业。

## 12. 发布钉子只认 `npm publish`，认不出另外七种发布方式

**缺口**：`tools/verify-mount.mjs` 扫 `.github/workflows/` 下每个文件，把带**活的**
`npm publish` 命令的那些当成发布工作流，然后钉它们：push 触发必须只认 `v*` tag、不能有
`branches:` 过滤、必须在同一个 job 里先无条件跑 `npm test`。

`host/git-guard.js` 认的发布方式比它多。它那条正则（`host/git-guard.js:238`）是：

```js
/\b(npm|pnpm|yarn|bun)\s+publish\b|semantic-release|release-please|gh\s+release\s+create|JS-DevTools\/npm-publish/i
```

除了 `npm publish` 本身，它还认**七**种：`pnpm publish`、`yarn publish`、`bun publish`、
`semantic-release`、`release-please`、`gh release create`、以及
`JS-DevTools/npm-publish`（一个 GitHub Action，不是 shell 命令，所以钉子那条找
「活的 `npm publish` 命令」的正则连形状都对不上）。**七这个数是按「用户会写下的写法」数的**：
`(npm|pnpm|yarn|bun)` 这一段是四种写法，减掉 `npm publish` 剩三种，加上后面四个选择项。
按正则的顶层选择项数只有五个——数法不同，数字就不同，所以这里把数法写出来。

**任何用这七种之一发布的工作流，在 guard 眼里是发布者，在钉子眼里什么都不是**——它不会被
钉，`ok` 行也不会提它。其中 `JS-DevTools/npm-publish` 最值得记住：它是这个仓库最可能用上
的那一种「文件里不出现 `npm publish` 这几个字」的发布方式。

**为什么没有用例**：不是写不出用例，是**不知道该断言什么**。T-44 拒绝了直接放宽：
`gh release create` 常常出现在一个 `workflow_dispatch:` 的手动工作流里，那种文件是对的，
一放宽就会把它误判成红——而「一道会把正确文件判红的门，会教人不再读它」是这个仓库自己写下
的理由（`tools/verify-mount.mjs` 里 T-44 那段注释）。所以要先有一个决定：这七种里哪些算
「必须先跑 `npm test`」的发布，哪些不算。

**该怎么办**：先决定（`CRD`），再改钉子，再补用例。今天这个仓库只用 `npm publish`，所以
**它自己是被盖住的**；这一条讲的是「换一种发布方式之后会静默失去保护」。

**状态**：未关闭，等一个决定。

## 12. `workflow_dispatch:` 配 tag-only 的 `push:`：人可以从任何分支手动发布

**缺口**：一个发布工作流写成这样，钉子和 guard 都不响：

```yaml
on:
  push:
    tags: ["v*"]
  workflow_dispatch:
```

- **钉子**只读 `push:` 那一块，看到 `tags: ["v*"]`、没有 `branches:`，判「tag-only」；
- **guard** 的 `branchPushTriggers()` 读的是同一块，得到同一个答案，于是放一个 crew 子
  agent 的普通分支推送过去；
- 但 `workflow_dispatch:` 让**任何有权限的人**在 GitHub 界面上选任意分支，手工跑这个
  工作流——于是从一个没有打 tag 的分支发布了一个版本。

**为什么没有用例**：和第 11 条同一个原因。`workflow_dispatch:` 本身是完全正常的东西
（重跑一次失败的发布、发一个 canary），把它一律判红会误伤正确的文件；把它判成「必须带
分支保护条件」需要读 `if:` 表达式里的 `github.ref`，那已经是要一个 YAML 解析器和一套
新规则了。**没有任何 DoD 条目要求过这件事**，所以这里只把洞写下来。

**该怎么办**：这是一条设计决定，不是一个用例：要么规定发布工作流不许有
`workflow_dispatch:`，要么规定它必须带一个限制分支的 `if:`。定了再加钉子和用例。
在那之前，**「钉子说 tag-only」的意思是「push 触发是 tag-only」，不是「只有 tag 能发布」**。

**状态**：未关闭，等一个决定。

## 13. 只数份数的钉子有余量时，掉一份没人看得见

**缺口**：`tools/verify-mount.mjs` 用「`docs/qa/gaps.md` 在 PM 提示词里至少出现 3 次」
钉住第 18 步的收尾搬运段落（`includes` 只看第一份，所以只能数）。今天
`roles/pm.md` 里有**4** 份。所以现在掉一份，计数还是 3，**门是绿的**。

`docs/qa/T-42/case-14-closing-migration-step-count.mjs` 把这件事说清楚了：它断言
「砍到 2 份必须红」「正好 3 份必须绿」——也就是钉住的是**下限**，不是「四份都在」。

**为什么没有用例**：把它写成「必须正好 4 份」会在下一次正当的编辑上变红（提示词加一句话
提到这个路径是完全正常的），那正是 `ADR 0004` 说的「会误红正确文件的门」。而要判「这四份
各在各自该在的段落里」，就要判段落的意思，那是第 2 条讲的事。

**该怎么办**：改 `roles/pm.md` 里任何提到 `docs/qa/gaps.md` 的段落时，别只看绿灯——门只
数数。真要更强，就给每一处加一个各自不同的、更长的锚点串（然后接受 `ADR 0004` 那份脆弱
性）。

**状态**：未关闭，按设计如此（宁可少一点保护，也不要一道会误红的门）。

## 14. `scripts.test` 的两个钉子只看紧跟在段落后面的那一个符号

**缺口**：`tools/verify-mount.mjs` 钉住 `package.json` 的 `scripts.test` 里两段命令
（`bash docs/qa/run-all.sh` 和 `node tools/verify-tasks.mjs`）：段落必须在，而且它的退出码
不许被扔掉。判「扔掉」的办法是看**紧跟在那一段后面**的符号是不是 `||`、单个 `|`、单个 `&`、
或者 `;` 后面还有别的命令。所以中间夹了任何东西，它就看不见了：

```
bash docs/qa/run-all.sh --quiet | tee log      <- 钉子读作绿
bash docs/qa/run-all.sh | tee log              <- 钉子读作红（符号紧跟着段落）
```

第一种写法里 QA 的用例照样跑，但退出码被 `tee` 吃掉，用例再也不能把 `npm test` 判红——
和这个钉子要防的那件事一模一样。

**为什么没有用例**：这不是「测不到」，是钉子本身盖不到；而**盖住它要读到那一段后面的下一个
`&&` 为止**——也就是要把整段命令切出来判，比 T-46 那个任务（修两个具体缺陷）宽得多。
T-46 把这个限制在源码注释里点了名，没有顺手放大自己的范围：一个顺手改宽的判断很容易在某个
正当的写法上误红，而「一道会把正确文件判红的门，会教人不再读它」是这个仓库自己的理由
（`ADR 0004`，第 11 条和第 13 条讲的是同一件事）。

**该怎么办**：先决定「一段命令到哪里结束」（读到下一个 `&&`），再改钉子，再补一条用例。
在那之前，**这两个钉子的意思是「段落在，而且紧跟着它的那个符号没有扔掉退出码」，不是
「这一段的退出码一定传得出来」**。给 `scripts.test` 里任何一段加参数或加重定向的时候，
自己看一眼退出码还在不在——绿灯不代表看过了。

**状态**：未关闭，等一个决定（不是等代码）。范围比 T-46 宽，所以那个任务没有做。

## 15. 靠 shell 活的角色，`crew_qa` 那一个没有被守着

**缺口**：`tools/verify-mount.mjs` 的那道「必须保住 `bash`」的检查，今天判的是一份**显式的
三名清单**：

```js
const NEEDS_SHELL = ["engineer", "test_engineer", "code_engineer"];
```

`crew_qa` **不在里面**，而 QA 一样靠 shell 活（它要跑项目的测试命令、要跑自己的用例）。
所以**把 `crew_qa` 的 `bash` 拿掉，一个检查都不会红**。

**为什么**：这不是漏掉，是**故意**的。把清单从一个角色扩到三个的那件作业
（`paired-engineers`，T-51）**不许改 QA 的任何行为**（`docs/design/prd-2026-08-21-paired-engineers.md`「不在范围内」），
而把 QA 约束进这条检查，本身就是一处新的行为约束。理由写在
`docs/decisions/adr/0010-bash-check-explicit-list.md`「它不证明什么」和
`docs/design/hld-2026-08-21-paired-engineers.md` 第五节，并且那个文件的注释里也写着。

`CLAUDE.md` 设计规则 4 记着的那个洞因此**从「三个里的一个」缩到「QA 一个」，没有关掉**。
（同一条规则的正文今天还写着「`verify-mount.mjs` checks the engineer's half only」——
那句话在 T-51 之后已经过时，改它归 T-60。）

**这个洞有没有安全后果**：没有。方向是「QA 少了一件工具」——能力减少、检查还打绿，
不是提权。它的代价是安静：QA 会在第一次要跑命令的时候才发现自己不能跑。

**该怎么办**：把 `crew_qa` 加进那份清单——**条件是有一件允许碰 QA 的作业**，因为往清单里加
名字等于给 QA 加一条行为约束。真要关，同一笔账还有两处也该一起看：`ADR 0010` 点名的
「第四个靠 shell 活的角色没人提醒你」（本清单是显式的），以及第 16 条那个 allow 列表的洞。

**状态**：未关闭，故意的。关掉它需要一件新作业。验法：`grep -n 'crew_qa' docs/qa/gaps.md`
（就是这一条），以及 `grep -n 'NEEDS_SHELL' tools/verify-mount.mjs` 看清单里有没有它。

## 16. 角色改用 allow 列表时，那道「必须保住 `bash`」的检查会打绿

**缺口**：判断写成（`tools/verify-mount.mjs`）：

```js
else if (role.deny?.includes("bash")) fail(`${role.toolName} must keep bash: …`);
```

一个角色从 deny 列表改成 allow 列表是**合法**的（同一个文件只要求「allow 和 deny 恰好有
一个」）。那时 `role.deny` 是 `undefined`，`?.includes("bash")` 也是 `undefined`——假值，
**不 fail**。接着计数没变，于是它打印：

```
ok    these roles keep the shell they work with: engineer, test_engineer, code_engineer
```

而同一个文件**禁止 allow 列表里出现 `bash`**（`CLAUDE.md` 设计规则 2）。所以那个角色
**确实**没有 shell 了，而这条专门为此存在的检查打了绿。

**为什么没有用例**：不是测不到，是**没人能修**。一行代码就能补（判「这个角色最终有没有
`bash`」，而不是判 deny 列表里有没有它），但 `tools/verify-mount.mjs` 归 **T-51**，
按所有权规则和 `ADR 0013`，T-51 交工之后别的任务一律不许再碰它。提出来的那份变更请求
（`docs/decisions/crd/0015-allow-list-shell-hole.md`）**被用户否决**（2026-08-21，原话
「no leave it for now」）。QA 不写一条钉住已知错误行为的用例——那会把这个洞变成一条要求。

**该怎么办**：另起一件作业，把判断改成看「最终有没有 `bash`」，并加一次变异测试
（把 `engineer` 改成 allow 列表，必须变红）。在那之前，**改任何角色的 allow / deny 形状的
人要自己看一眼那个角色还有没有 shell**——绿灯在这件事上不作数。

**状态**：未关闭，已知且故意留着（用户决定）。

## 17. `ADR 0010` 要给 `CLAUDE.md` 补的那一步——**已经关闭，而且比 `ADR 0010` 要的更准**

**缺口（原文，留着当记录）**：`tools/verify-mount.mjs` 里有**三份显式清单**，加一个新角色时
要改的不止一份：`NEEDS_SHELL`（靠 shell 活的角色，`verify-mount.mjs:682`）、CRD 0006 那份
文件名清单、CRD 0010 那份文件名清单。`CLAUDE.md` 的「Adding or changing a role」那份步骤
清单里**没有这一步**，所以加角色的人不会被提醒，而漏掉的后果是**那个角色没有任何检查看着它**。

**这一条本来写反了，2026-08-21 由最后一轮 QA 改准。** 旧文写着「只被承载了三分之一」、
「两份文件名清单仍然没有任何 DoD 承载」、「状态：未关闭」。**那是 T-60 交工之前的状态。**
T-60 已经把这一步真的写进 `CLAUDE.md`（「Adding or changing a role」的**第 5 步**），
而且它点名了三份清单，一份都不少：

> The three: if the role lives by the shell, its **role key** goes in the shell list of design
> rule 4. Then its **persona file name** goes into each of the two file-name lists whose rule
> reaches that role …

**它把措辞收紧了，而这不是打折，是改对了。** `ADR 0010` 的说法读起来像「三份都无条件加」，
而 T-60 写的是「**哪条规则管得到这个角色，就加进那一份**」。QA 核过两份清单的实际内容
（2026-08-21）：CRD 0006 那份是 5 个 persona（`engineer.md`、`test-engineer.md`、
`code-engineer.md`、`architect.md`、`doc-reviewer.md`，`verify-mount.mjs:523`），
CRD 0010 那份是 8 个（`verify-mount.mjs:572`）。**两份都不含每一个角色**——`researcher.md`
不在任何一份里，`qa.md` 不在 CRD 0006 那一份里。所以「三份都无条件加」会让下一个加角色的人
把 `researcher.md` 塞进一份管不到它的清单，制造一条假要求。**收紧之后的说法是可执行的，
原来那句不是。**

**为什么这一条仍然没有用例**：这是一条要写进文档的步骤，不是一段行为——用例判不了「加角色的人
真的照做了」。能判的是**那一步在不在**，而这一条现在有常驻用例了：
`docs/qa/T-60/case-08-adding-a-role-has-the-new-step.mjs` 钉着那一步在、三份清单都被点名、
两份文件名清单是**按规则管得到才加**、以及它指向 `ADR 0010` 和那两条 CRD。
`CRD 0015` 第二条当初提议补齐这件事、**被用户否决**（2026-08-21，「no leave it for now」），
后来是 T-60 的 DoD 第 6 条把它接了回来。

**状态**：**已关闭。** 三份清单在 `CLAUDE.md` 第 5 步里全部被点名，措辞按两份清单的实际
成员收紧，并有 `docs/qa/T-60/case-08` 常驻守着。**剩下的部分永远归人**：没有任何检查能证明
加角色的人真的去改了那三份清单——`ADR 0010`「它不证明什么」那一节说的就是这件事，那句话仍然
成立。

## 18. 「今天必须这样、以后必须不这样」的断言，进不了回归套件

**缺口**：有些验收检查是**一次性的**，它们的真值会在后面某个里程碑翻过来。这类断言
**不许**写成常驻用例，因为 `docs/qa/run-all.sh` 每次把过去所有作业的用例全跑一遍
（那是它存在的理由），而每个任务又要求 `npm test` 全绿——两条要求会在别人的任务上互相抵死。
本作业（`paired-engineers`）里有两处：

- **两份新 persona 各有一行 `M1-PLACEHOLDER: …`**。M1 要求它**在**，M3（T-53、T-54）要求它
  **没了**。`ADR 0013` 因此明令：「必须在」这个方向**只由人在里程碑评审时跑一次**
  （`grep -n 'M1-PLACEHOLDER' roles/test-engineer.md roles/code-engineer.md`，M1 评审时各 1 处），
  常驻用例只写「已经没了」这个方向，住在 `docs/qa/T-53/`、`docs/qa/T-54/`。
- **`roles/` 下 `worktree` 和 `merged tree` 今天各 0 处**。T-53 第 4 条、T-54 第 2、6 条正是
  靠这个 0 来证明「那个词一定是这次写下的」。**M1 结束时由人数一次**（2026-08-21：`roles/*.md`
  十份文件，两个词各 0 处），M3 之后它会变成非 0，所以它也不能是常驻用例。

**为什么没有用例**：用例是回归工具，它只装**单向**的断言——一旦为真、永远为真的那种。
一条会在计划好的日子里翻面的断言，属于里程碑评审，不属于回归套件。

**该怎么办**：写这类验收检查的时候，**在 DoD 那一格里就写清由人跑一次**（T-51 第 10 条就是
这么写的），并把反方向那条常驻用例挂到会让它翻面的那个任务上。QA 在报告里贴出人跑的真实
数字，那就是它唯一的记录。

**状态**：未关闭，按设计如此（`ADR 0013` 的一句话值得记住：**常驻用例只装单向的断言**）。

## 19. 配置里的角色键没有任何校验：拼错就静默失效

**缺口**：`~/.dsh/.agent-presets/crew/agent.cordis.yml` 里的 `roleAllow`、`roleDeny`、
`roleModels` 都是「角色键 → 值」的对象，而**那个键本身没有任何检查**。写错一个字母，
那一整行就被**静默忽略**——启动日志一个字都不说，用户以为自己的配置生效了，实际生效的是
出厂列表。

`CRD 0016` 这一轮补上的空数组校验**没有改变这一点**，也没有被要求改变它。校验写在
`host/roles-preset.js` 里「按 `ROLES` 逐个角色」的循环里（`for (const role of ROLES)`，
再取 `config?.[field]?.[role.key]`），所以它只看得见**真实存在的**角色键：

- `roleDeny: { engineer: [] }` → 被抓到，挂载期报错（DoD 第 19 条，QA 的 `case-18` 钉着）；
- `roleDeny: { enginer: [] }` → **抓不到**。循环里没有 `enginer` 这个 key，那一项从头到尾
  没被读过，挂载照旧成功。

这一条是 T-51 的 engineer 自己诚实报出来的，QA 复核确认：它**既不是这一轮引入的，也没有
被这一轮变严**。今天的净结果是——**空数组会被拒，拼错的键不会**。

**为什么没有用例**：`CRD 0016` 只定了「空列表必须拒绝启动」，`docs/design/tasks.md` 的
T-51 第 19 条也只写了这一件事。**堵不堵拼错的键是另一条要求，还没有人提。** QA 不为一条
文档没有要求的行为写常驻用例——那等于 QA 自己发明验收标准；也不写一条钉住「拼错必须被静默
忽略」的用例——那会把今天这个洞变成一条要求（同 `gaps.md` 第 16 条的道理）。

**这一段的措辞在 2026-08-21 改准过一次**（`CRD 0016` 的追加一节落地之后）：原来写着
「不是数组的值（空字符串、`~`、一个字符串而不是列表）同样被静默忽略或直接交给下游」，
那句话**现在不再成立**。校验已经放宽判据：`undefined` 和 `null`（YAML 里的 `~` 或留空）
是「用出厂列表」的正确写法，**放过**；`[]`、`""`、`0`、`false`、`{}`、以及非空但不是列表的
值（`"read"`）**一律拒绝启动**（QA 的 `case-21`、`case-24` 钉着九个角色键 × 两个字段 ×
六种形状）。所以**值的形状已经被校验过了，键还没有**——这一条剩下的缺口只有键。

**该怎么办**：另起一件作业，在挂载期校验一次配置的形状——`roleAllow` / `roleDeny` /
`roleModels` 的每一个键都必须能在 `ROLES` 里找到，找不到就报错并列出已知的键
（形状照 `tools/verify-mount.mjs:689` 那条「清单过期了」的错误信息，它解决的是同一个
失败模式）。注意 `host/roles-preset.js` 归 T-51，交工之后按所有权规则和 `ADR 0013`
别的任务不许再碰它，所以这件事只能是一件**新作业**，不能塞进现有任务。

**状态**：未关闭。没人要求关，也没人认领。空数组已堵（`CRD 0016`），拼错的键仍然静默失效。

## 20. 一份单次文档**实际落在哪个目录**，没有任何检查看得见

**缺口**：这个仓库最强的两道钉子——`CRD 0006`（按寿命分家）和 `CRD 0010`（DoD 一节住在
任务行里）——在 `tools/verify-mount.mjs` 里都是**显式文件名清单加子串查找**：某份 persona
里必须出现 `docs/decisions/adr/`、`docs/design/tasks.md`、`DoD section`，必须不出现
`dod.md`、`**Decisions** section`。它们查的是**提示词里那句话还在不在**，查不到**一份真实
的文件被写到了哪里**。所以「单次的东西不许住进耐久的家」这条规则，只有它的**说法**被守着，
它的**结果**一个字都没有被守着。

真实发生过一次，就在钉子最密的地方：`roles/qa.md` 加粗写着 QA 的测试计划
「goes in the job folder, outside the repository … never in the repository」，而 QA 上一轮
把计划写成了 `docs/qa/T-51-plan.md`——**写进了仓库，而且写进了 QA 自己的家**。
`node tools/verify-mount.mjs` 那一轮 79 项检查全绿，`npm test` 全绿，
`bash docs/qa/run-all.sh` 六个任务全过。**没有任何一条红。** 是 T-51 的 engineer 读文件时
用眼睛看见的，不是任何检查看见的。这正是 `CRD 0006` / `CRD 0010` 存在要防的那种先例：
一份单次文档在耐久目录里存活一轮，下一个人就会照着它的位置再放一份。

**为什么没有用例**：判「这份文档该住哪」需要两样东西，两样今天都不在 T-51 的判据范围里——
一是一份**目录白名单**（哪些路径是耐久的家、哪些是单次的），二是一次**全仓库扫描**（而不是
读几个点名的文件）。写成常驻用例还有两个真问题：作业文件夹在仓库外面（`~/.claude/crew/jobs/`），
用例按规矩不许读用户主目录里的东西；而「`docs/qa/` 下不许有 `*-plan.md`」这种钉法只挡住
**这一个**文件名，换个名字（`T-51-notes.md`、`plan-T-51.md`）就绕过去了——那是假绿灯，
`gaps.md` 第 2 条讲的就是这种钉法的下限。

**该怎么办**：两条，成本都不高。
1. 加一条**否定形状**的检查（一条 `git ls-files` 加一个模式表就够）：`docs/qa/` 下只许有
   `gaps.md`、`run-all.sh`、`lib/`、和 `T-*/` 里的 `case-*`、`run.sh`、非 case 的辅助模块；
   多出来的文件就红，并说清「单次文档不住这里，去作业文件夹」。它是**结果**的检查，不是
   说法的检查，所以它挡的是下一次真实的越界，而不是下一次的措辞漂移。这需要一件新作业：
   `tools/verify-mount.mjs` 归 T-51，交工之后别的任务不许再碰（`ADR 0013`）。
2. 在那之前，**由人核**：每个任务交工时，`git status --short docs/` 看一眼有没有不该出现的
   文件。这一轮的记录：QA 交工时 `docs/qa/` 下只有 `gaps.md`、`run-all.sh`、`lib/`、
   六个 `T-*/`；上一轮那份 `docs/qa/T-51-plan.md` 已经被 PM 移到作业文件夹里。

**状态**：未关闭。第 2 条（人核）从这一轮开始有记录；第 1 条（检查）没人认领，需要一件
新作业，因为它要动 `tools/verify-mount.mjs`。

## 21. 一句散文里的「test / 测试」用词对不对，没有任何检查能判

**缺口**：`principles.md` 的 `Words we use` 那一节（T-52 加的）定了四个精确名词——
单元测试（unit test）、QA 用例（case）、项目的测试命令、契约测试（contract test）——外加一条
硬规则：**一句话如果可能指其中两样，必须用精确名词；光写「test / 测试」只允许出现在故意指
「任意一种」的地方**。**这条规则本身没有任何检查能判。**

能被检查的只有**存在性**，QA 的 `docs/qa/T-52/` 钉的就是这几样：

- 那一节在、标题是 `Words we use`、不编号、位置在原则 21 之后（`case-09`）；
- 表里四条齐全、四列都填了（`case-10`）；
- 那条规则的句子在（`case-11`）；
- 禁令「不要写 QA test」在，而且 `principles.md` 里 `QA test` 的每一处命中都在那条禁令里，
  `roles/*.md` 里 0 处（`case-12`）。

**判不了的是**：文件里任何一句话用的名词对不对。`principles.md` 一千三百多行，
`roles/pm.md` 一千两百多行，里面的 bare「test」有的**故意**是「任意一种」（原则 6 的标题就是，
那一节自己说明了），有的只是没被清理到（PRD v3、`ADR 0014` 定的清理边界只到原则 6、原则 21、
用词表本身，以及本作业本来就要动的那几个文件）。**两者长得完全一样**，一个用例区分不了
「故意的 bare test」和「漏掉的 bare test」。

`ADR 0014`「它可以被检查」那一段点名要求把这一条记在这里：
「**没有任何检查能证明某一句散文里的『测试』用词正确**，这一条要进 `docs/qa/gaps.md`」，
承载点是 T-52 的 DoD 第 18 条。

**为什么没有用例**：用例能断言一个串在不在，判不了一句话的意思对不对（同第 1、2 条）。
往这里硬写一个用例，只有两种写法，两种都坏：
数 bare「test」的个数——那会把今天的漏改次数固化成一条要求，改对了反而红（同第 16 条的道理）；
或者禁掉 bare「test」——那会误伤原则 6 的标题和用词表自己那条规则里的引用，而它们是**对的**。

**还有一层，钉散文的串这件事本身也会坏，而这件作业里它一共咬了八次**（同一个缺口的实例，
按本文件的规矩不另开一条，只把话补准）：`principles.md` 和 `roles/*.md` 的散文按 **80 列**
折行，`CLAUDE.md` 和两份 README 按 100 列，所以**一句话通常横跨两三行**，`grep` 和任何按行
匹配的用例都会把一句**真的在**的话判成不存在。

（**这一段里的「80 列」四个字是被钉住的**：`docs/qa/T-52/case-18` 按
`/80 列/` 匹配这一条。最后一轮 QA 改这一条时先把它写成「80 到 100 列」，那个用例当场变红——
这正是回归套件该有的反应，也是一个刚好的例子：**钉子该守着的是我的文件，不是我的记性。**
改法是把我的文字改回带「80 列」，不是去改那个用例。）

**八次，engineer 五次、PM 三次**（2026-08-21，一天之内）。已经记下的三次：engineer 第一版把
`**DoD section**` 折了行；PM 给出的原文里 `roleDeny: {` 撞坏了 T-51 的 `case-04`；
PM 按行 grep 一句跨行的话，得到空结果。

**第七次要单独写，因为它长在 PM 写的 DoD 验法上，而且它从写下那一刻就永远为空。**
`docs/design/tasks.md` 里 T-60 第 7 条的验法是：

> `grep -n 'no \`prd.md\`' CLAUDE.md` **为空**

最后一轮 QA 核过（用 `git show a4828b7:CLAUDE.md`，即 T-60 动手**之前**那一版）：
`grep -c 'no \`prd.md\`'` 得到 **0**。原文在第 216-217 行折了行——`There is no` 在行尾，
`` `prd.md` `` 在下一行行首。**所以这一格在 T-60 干任何活之前就已经是绿的，它不可能变红，
它证明不了任何事。** 一个验法如果不可能失败，它不是验法。这是本文件第 16 条那个病的另一个形状：
**打绿的检查什么也没看**。

**该怎么办（一条现成的做法，不要再靠记性）。** 钉一句散文之前先把文件展平
（`text.replace(/\s+/g," ")`，`docs/qa/lib/qa.mjs` 的 `flat`、
`docs/qa/T-52/principles.mjs` 的 `flatten`），或者只钉不会折行的东西——`## ` 标题、
`| … |` 表格行、单个名词、路径。

**再加一步：数两遍，两个数必须一致。** 这是 T-59 想出来的办法，现在写进这里：把同一个串
**展平之后数一遍，再逐行数一遍**。

```js
const flatHits = flatText.split(needle).length - 1;              // 展平之后
const lineHits = text.split("\n").filter((l) => l.includes(needle)).length; // 逐行
```

**展平的数比逐行的数大 → 这个串折了行，任何按行匹配的钉子都在说谎**（它会漏掉那一处，
而漏掉一处的方向决定了它是假绿还是假红）。两个数相等，按行钉才可信。
`docs/qa/T-60/case-09-prd-and-hld-exist-now.mjs` 里有一份能跑的实现，就钉在上面那个
「从写下就为空」的串上；它同时把两个数打印成一行 `note`，所以下一个人读到的是数字，不是叮嘱。
**`docs/qa/T-52/`、`T-53` 到 `T-62` 的每个用例头部都写明了自己用的是哪一种**，因为下一个人
会踩同一个坑。

**该怎么办（用词这件事）**：要一个读者，不是一个用例——`crew-doc-reviewer` 读原则 6、原则 21
和用词表那三节（按 `CRD 0018`，这一轮的文档评审批到最后一程，所以 T-52 交工时**还没有人读过**
这三节的用词）。存在性那几条已经有常驻用例守着，剩下的永远归人。

**状态**：未关闭，而且不会被用例关闭。这是「用例能判什么」的边界，不是技术问题。

---

## 22. `CHANGELOG.md` 的段落顺序，现在没有用例守着了

**缺口**：这个文件「最新的在最上面」、各版本小节从新到旧、最上面那一段的版本号和
`package.json` 对得上——这三件事没有任何用例判。发 `0.8.0` 的时候，`T-61` 的
`case-04-unreleased-is-at-the-top.mjs` 被删掉了，这三条检查跟着一起走了。

**为什么**：那个用例钉的是 `headings[0] === "## unreleased"`，也就是「这一段还没发布」。
它的理由写在它自己的文件头上：本次作业不发版，两个新角色还没上 npm，用户读到一个带日期的
版本小节会以为能装。发布这件事本身让这个前提不成立——给那一段加上日期，正好是它要拦的动作。
它同时也是顺序检查的唯一住处，所以理由过期的时候，顺序检查也一起过期了。这是用例贴着
「此刻的状态」写、而不是贴着「永远成立的规则」写的代价，和第 18 条是同一类。

**该怎么办**：下一次再往这个文件加未发布的一段时，把顺序那几条重写成不依赖「未发布」这个
状态的说法——最上面那一段是不是带日期的版本号、它和 `package.json` 对不对得上、各段是不是
从新到旧。写成常驻用例，不要再挂在某一次作业的任务号下面。

**状态**：未关闭。是有人挑的，不是漏的：`0.8.0` 发布时用户在两个选项（重写、删掉）里选了删掉。
